# Configure the controller manager deployment.
manager:

  # -- Image settings for the controller manager
  image:
    repository: registry.lab.dynatrace.org/research/koney-controller # patch:manager
    tag: 0.2.2
    pullPolicy: IfNotPresent
  # -- Arguments
  args: []
  # -- Environment variables
  env: []
  # -- Pod-level security settings
  podSecurityContext:
    runAsNonRoot: true
    seccompProfile:
      type: RuntimeDefault
  # -- Container-level security settings
  containerSecurityContext:
    allowPrivilegeEscalation: false
    capabilities:
      drop:
      - ALL
  # -- Number of controller manager replicas
  replicas: 1
  # -- Resource limits and requests
  resources:
    limits:
      cpu: 500m
      memory: 256Mi
    requests:
      cpu: 10m
      memory: 128Mi

# Configure the alert forwarder deployment.
# TEMPORARY VALUES - DO NOT USE !!!
# WILL BE REMOVED ONCE #18 IS DONE.
alertForwarder:

  # -- Image settings for the alert forwarder
  image:
    repository: registry.lab.dynatrace.org/research/koney-alert-forwarder # patch:alert-forwarder
    tag: 0.2.2
    pullPolicy: IfNotPresent

  # Shared secret that the alert forwarder uses to authenticate the callers of its webhooks.
  # Koney appends the token to the webhook URLs that it hands out to Tetragon and Kive.
  webhookToken:

    # -- Store the token in a Secret (the token of a previous release is reused on upgrades)
    create: true
    # -- Use this token instead of generating a random one
    value: ""

# Helper RBAC roles for managing custom resources
# These provide convenient admin/editor/viewer roles for each CRD type
# Useful for giving users different levels of access to your custom resources
rbacHelpers:

  # -- Install convenience admin/editor/viewer roles for CRDs
  enable: false

# Cleanup hooks.
# Removes all DeceptionPolicy and DeceptionAlertSink resources before uninstalling Koney.
# Ensures that finalizers are executed and traps are cleaned up before the controller is removed.
cleanup:

  # -- Deploy the pre-delete cleanup hook
  enable: true

# Custom Resource Definitions.
crd:

  # -- Install CRDs with the chart
  enable: true
  # -- Keep CRDs when uninstalling
  keep: false

# Controller metrics endpoint.
metrics:

  # -- Enable to expose /metrics endpoint with RBAC protection.
  enable: true
  # -- Metrics server port
  port: 8443

# Prometheus ServiceMonitor for metrics scraping.
# Requires prometheus-operator to be installed in the cluster.
prometheus:

  # -- Enable ServiceMonitor creation
  enable: false

# Configure manifest specific labels.
# You probably never want to change these unless you want
# to generate manifests with other templating tools.
template:

  # -- Add Helm-specific labels to all resources
  helmLabels: true

  # -- Create an explicit namespace resource
  createNamespace: false
